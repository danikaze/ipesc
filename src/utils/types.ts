export type Timestamp = number;
export type IsoDate = string;
export type LapTimeAsString = string;
export type LapTimeAsMs = number;
export type NumberAsString = string;

export const enum BooleanInt {
  TRUE = 1,
  FALSE = 0,
}
